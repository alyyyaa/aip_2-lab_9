# На координатной плоскости рисуются окружности, у каждой окружности следующие параметры: координаты X и Y - центра окружности, 
# и значение R - это радиус окружности. По умолчанию центр находится в (0, 0), а радиус равен 1.
# Реализуйте класс Окружность, который инициализируется по этим параметрам. Круг также может:
# - Находить и возвращать свою Площадь
# - Находить и возвращать свой Периметр
# - Увеличиваться в K раз
# - Определять, пересекается ли он с другой окружностью.
# Написать меню, которое будет иметь следующий вид:
# 1)	Создать окружность
# 2)	Найти площадь окружности 
# 3)	Найти периметр
# 4)	Увеличить радиус окружности
# 5)	Определить пересечение окружности с номером N с окружностью с номером M
# 6)	Выход

import math
class Circle:
    def __init__(self, x=0, y=0, r=1):
        self.x = x
        self.y = y
        self.r = r
    def area(self):
        return math.pi * self.r ** 2
    def perimeter(self):
        return 2 * math.pi * self.r
    def increase(self, k):
        self.r *= k
    def intersection(self, other):
        return math.sqrt((self.x - other.x) ** 2 + (self.y - other.y) ** 2) <= self.r + other.r

circles = []

while True:
    print("Меню:")
    print("1) Создать окружность")
    print("2) Найти площадь окружности")
    print("3) Найти периметр окружности")
    print("4) Увеличить радиус окружности")
    print("5) Определить пересечение окружности с номером N с окружностью с номером M")
    print("6) Выход")

    choice = int(input("Введите действие: "))

    if choice == 1:
        x = float(input("Введите координату X центра окружности: "))
        y = float(input("Введите координату Y центра окружности: "))
        r = float(input("Введите радиус окружности: "))
        circle = Circle(x, y, r)
        circles.append(circle)
        print("Окружность создана")

    elif choice == 2:
        n = int(input("Введите номер окружности: "))
        print("Площадь окружности:", circles[n - 1].area())

    elif choice == 3:
        n = int(input("Введите номер окружности: "))
        print("Периметр окружности:", circles[n - 1].perimeter())

    elif choice == 4:
        n = int(input("Введите номер окружности: "))
        k = float(input("Введите коэффициент увеличения радиуса: "))
        circles[n - 1].increase(k)
        print("Радиус окружности увеличен в", k, "раз")

    elif choice == 5:
        n = int(input("Введите номер первой окружности: "))
        m = int(input("Введите номер второй окружности: "))
        if circles[n - 1].intersection(circles[m - 1]):
            print("Окружности пересекаются")
        else:
            print("Окружности не пересекаются")

    elif choice == 6:
        print("Выход")
        break
    else:
        print("Неверное действие")
